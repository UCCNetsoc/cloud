import { Cloud } from "../types";
import request from "./request";

// export const GetVhostRequirements = async (): Promise<Cloud.VHostRequirements | null> => {
//     const response = await request(`/vhost-requirements`, { method: "GET" });
//     return response[1];
// }
